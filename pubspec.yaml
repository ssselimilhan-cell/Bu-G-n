name: bugun
version: 0.1.0+1
description: Ankara'da bugün ne var? - günlük şehir keşif uygulaması
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.1
  intl: ^0.19.0
  geolocator: ^13.0.2
  google_maps_flutter: ^2.12.3
  cached_network_image: ^3.4.1
  go_router: ^16.0.0
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true